{
  "name": "bahdahshin-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "vite --host 0.0.0.0", "build": "vite build", "preview": "vite preview --host 0.0.0.0", "sync": "node scripts/sync.mjs", "test": "node --test" },
  "devDependencies": { "vite": "^7.1.0", "@playwright/test": "^1.55.0" }
}
